version: '3.8'
services:
  app:
    build: .
    ports:
      - "8082:80"
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_NAME=myapp_db
      - DB_USER=root
      - DB_PASS=password
  db:
    image: mariadb:10.5
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: myapp_db
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: